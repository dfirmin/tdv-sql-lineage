[project]
name = "tdv-sql-lineage"
version = "0.1.0"
description = "Static lineage extraction for Python ETL scripts with embedded SQL"
readme = "README.md"
authors = [{ name = "AutoGenerated" }]
requires-python = ">=3.10"
dependencies = [
    "sqlglot>=23.0.0,<24.0.0",
]

[project.scripts]
tdv-sql-lineage = "lineage.cli:main"

[tool.uv]
managed = true
